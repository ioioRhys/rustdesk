name: Manual Build (Win x64 + Android Universal)

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: nightly
    secrets: inherit